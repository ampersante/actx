{
  "comment": "TK-41 mechanical golden corpus (H-F10/N-F12a). Verdicts of the PRE-migration code (base a4a1a2b: rewriter._docker_ok/_DOCKER_RO, hang_policy._is_docker, T6 non-cloud docker entry), captured by running rewriter.rewrite()/hang_policy.classify() over the corpus and committed BEFORE the cli_families migration. Cases whose verdict the TK-41 spec prescribes to change are listed in PRESCRIBED_CHANGES in tests/test_docker_golden.py at the commit that changes them; every other verdict must match this dump forever.",
  "hang": {
    "docker --context prod compose up -d": "default",
    "docker attach": "never_wrap",
    "docker compose -f x.yml up": "never_wrap",
    "docker compose -f x.yml up -d": "default",
    "docker compose attach x": "never_wrap",
    "docker compose logs": "default",
    "docker compose logs --follow web": "default",
    "docker compose logs -f": "default",
    "docker compose ps": "default",
    "docker compose up": "never_wrap",
    "docker compose up --detach": "default",
    "docker compose up -d": "default",
    "docker logs -f": "never_wrap",
    "docker ps": "default",
    "docker stats": "never_wrap",
    "docker stats --no-stream": "default"
  },
  "rewrite": {
    "docker --context prod images": "none",
    "docker --context prod logs": "none",
    "docker --context prod ps": "none",
    "docker compose -f x.yml ps": "none",
    "docker compose -f x.yml ps -a": "none",
    "docker compose logs": "none",
    "docker compose logs -f": "none",
    "docker compose logs web": "none",
    "docker compose ps": "prefix",
    "docker compose ps -a": "prefix",
    "docker compose up": "none",
    "docker compose up -d": "none",
    "docker exec x ls": "none",
    "docker images": "prefix",
    "docker images -a": "prefix",
    "docker inspect --size c": "none",
    "docker inspect c": "none",
    "docker inspect c1 c2": "none",
    "docker logs": "prefix",
    "docker logs --tail 50 web": "prefix",
    "docker ps": "prefix",
    "docker ps -a": "prefix",
    "docker ps -n 5 web": "prefix",
    "docker rm x": "none",
    "docker rmi x": "none",
    "docker run x": "none",
    "docker stats": "none",
    "docker stats --no-stream": "none",
    "docker stats -a --no-stream": "none",
    "docker system df": "none",
    "docker system df -v": "none",
    "docker system prune": "none",
    "docker volume prune": "none",
    "docker volume rm x": "none"
  }
}
